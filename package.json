{
  "name": "regenesis-scraper",
  "version": "0.1.0",
  "scripts": {
    "bundle": "browserify lib/js/src/Handler.js --standalone app -g uglifyify --node > app.js",
    "build": "bsb -make-world",
    "local": "AWS_REGION=us-east-1 serverless offline start --stage dev --verbose",
    "deploy": "serverless deploy",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1064.0",
    "bs-abstract": "^1.0.0",
    "bs-effects": "^0.10.0",
    "bs-platform": "^7.3.2",
    "cheerio": "^1.0.0-rc.10",
    "got": "^11.8.5",
    "tough-cookie": "^3.0.1"
  },
  "devDependencies": {
    "browserify": "^16.5.2",
    "serverless": "^3.0.0",
    "serverless-offline": "^8.3.1",
    "serverless-prune-plugin": "^2.0.1",
    "uglifyify": "^5.0.2"
  }
}
